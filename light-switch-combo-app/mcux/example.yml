# yaml-language-server: $schema=../../../../scripts/data_schema/example_description_schema.json
#
# Copyright 2024-2025 NXP
#
# SPDX-License-Identifier: BSD-3-Clause

# This file describes the Matter Light switch combo example application for the NXP MCUXpresso SDK.

light-switch-combo-app:
  section-type: application
  contents:
    project-root-path: examples/matter_examples/light-switch-combo-app/mcux
    meta_path: examples/matter_examples/light-switch-combo-app/mcux
    document:
      name: light-switch-combo-app
      category: matter_examples
      brief: The NXP Light switch combo example is based on Project CHIP and NXP SDK,
        and provides a prototype of a light switch combo device which demonstrates device 
        commissioning and different cluster control.
      example_readme:
      - examples/matter_examples/light-switch-combo-app/README.md
    extra_files:
    - source: ${SdkRootDirPath}/middleware/matter/examples/light-switch-combo-app/light-switch-combo-common
      destination: zap
    - source: ${SdkRootDirPath}/middleware/matter/examples/light-switch-combo-app/nxp/common
      destination: common
    - source: ${SdkRootDirPath}/middleware/matter/third_party/nxp/nxp_matter_support/examples/platform/rt/nxp_sdk_reconfig.cmake
      destination: nxp_sdk_reconfig.cmake
      board: [rdrw612bga, frdmrw612]
    - source: ${SdkRootDirPath}/middleware/matter/third_party/nxp/nxp_matter_support/examples/platform/rt/rw61x/app/ldscripts/RW610_flash.ld
      destination: ldscripts/RW610_flash.ld
      board: [rdrw612bga, frdmrw612]
    replacements:
    - file: CMakeLists.txt
      replace:
        - from: "get_filename_component(NXP_EXAMPLE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../common REALPATH)"
          to: "get_filename_component(NXP_EXAMPLE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/common REALPATH)"
    - file: CMakeLists.txt
      replace:
        - from: "get_filename_component(NXP_EXAMPLE_ZAP_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../zap REALPATH)"
          to: "get_filename_component(NXP_EXAMPLE_ZAP_DIR ${CMAKE_CURRENT_SOURCE_DIR}/zap REALPATH)"
    - file: CMakeLists.txt
      replace:
        - from: "get_filename_component(NXP_SDK_RECONFIG_CMAKE_DIR ${NXP_MATTER_SUPPORT_DIR}/examples/platform/${CONFIG_CHIP_NXP_PLATFORM_FAMILY} REALPATH)"
          to: "get_filename_component(NXP_SDK_RECONFIG_CMAKE_DIR ${CMAKE_CURRENT_SOURCE_DIR} REALPATH)"
    - file: nxp_sdk_reconfig.cmake
      replace:
        - from: "BASE_PATH ${MATTER_DEFAULT_LINKER_FILE_PATH}"
          to: "BASE_PATH ${CMAKE_CURRENT_SOURCE_DIR}"
      board: [rdrw612bga, frdmrw612]
    - file: nxp_sdk_reconfig.cmake
      replace:
        - from: "LINKER ${MATTER_DEFAULT_LINKER_FILE_NAME}"
          to: "LINKER ldscripts/RW610_flash.ld"
      board: [rdrw612bga, frdmrw612]
    freestanding_copied_folders:
    -  middleware/matter/third_party/nxp/nxp_matter_support/examples/platform/project_segments
    toolchains:
    - +armgcc@debug
    - +armgcc@release
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@flexspi_nor_debug
    - -armgcc@flexspi_nor_release
    - -armgcc@sdram_debug
    - -armgcc@sdram_release
    - -iar@debug
    - -iar@release
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@flexspi_nor_debug
    - -iar@flexspi_nor_release
    - -iar@sdram_debug
    - -iar@sdram_release
    - -mdk@debug
    - -mdk@release
    - -mdk@flash_debug
    - -mdk@flash_release
    - -mdk@flexspi_nor_debug
    - -mdk@flexspi_nor_release
    - -mdk@sdram_debug
    - -mdk@sdram_release
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Wi-Fi with Factory Data Configuration ---
light-switch-combo-app@wifi_factory_data:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_wifi_fdata.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Wi-Fi OTA Configuration ---
light-switch-combo-app@wifi_ota:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_wifi_ota.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Wi-Fi OTA with Factory Data Configuration ---
light-switch-combo-app@wifi_ota_factory_data:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_wifi_ota_fdata.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Thread FTD Configuration ---
light-switch-combo-app@thread_ftd:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_thread_ftd.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Thread FTD with Factory Data Configuration ---
light-switch-combo-app@thread_ftd_factory_data:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_thread_ftd_fdata.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Thread FTD OTA Configuration ---
light-switch-combo-app@thread_ftd_ota:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_thread_ftd_ota.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Thread FTD OTA with Factory Data Configuration ---
light-switch-combo-app@thread_ftd_ota_factory_data:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_thread_ftd_ota_fdata.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Thread FTD + Wi-Fi Border Router OTA Configuration ---
light-switch-combo-app@thread_ftd_wifi_border_router_ota:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_thread_ftd_wifi_br_ota.conf
  boards:
    frdmrw612: []
    rdrw612bga: []

# --- Thread FTD + Wi-Fi Border Router OTA with Factory Data Configuration ---
light-switch-combo-app@thread_ftd_wifi_border_router_ota_factory_data:
  section-type: custom_application
  contents:
    document:
      extra_build_args:
      - -DCONF_FILE=middleware/matter/examples/platform/nxp/config/prj_thread_ftd_wifi_br_ota_fdata.conf
  boards:
    frdmrw612: []
    rdrw612bga: []
